import SwiftUI

struct RootView_Dummy: View {
    var body: some View {
        Text("ROOTVIEW DUMMY OK")
            .font(.largeTitle)
            .padding()
            .background(Color.green.opacity(0.5))
    }
}
